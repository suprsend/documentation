---
title: "Reset Schema"
description: "Reset a live schema to draft in a SuprSend workspace."
keywords:
  - suprsend schema reset
  - reset schema suprsend
  - suprsend cli schema
  - suprsend cli reference
---

Revert a live schema back to draft in a SuprSend workspace.  
Useful for testing or modifying an active schema.

```bash
suprsend schema reset [flags]
```

## Options

| Flag         | Description               | Default |
| ------------ | ------------------------- | ------- |
| `-h, --help` | Show help for the command | –       |

### Inherited Global Flags

This command also supports [Global Flags](/reference/cli-global-flags), such as:

* `--workspace` – Workspace to use (default: `staging`)
* `--service-token` – Service token for authentication
* `--output` – Output format (`pretty`, `json`, `yaml`)
* `--verbosity` – Logging level
* `--config` – Config file path

## Examples

Reset a schema in staging:

```bash
suprsend schema reset --workspace staging
```

Reset a schema in production:

```bash
suprsend schema reset --workspace production
```

## See Also

* [Schema Commands](/reference/cli-schema-overview) – overview of all schema commands
* [Commit Schema](/reference/cli-schema-commit) – commit schema from draft to live
* [List Schemas](/reference/cli-schema-list) – list all schemas in a workspace
