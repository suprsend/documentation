---
title: Create Workspace API Key
openapi: "POST /v1/{workspace}/ws_api_key/"
sidebarTitle: Create API Key
---

<Warning>
  The full `api_key` is returned **only in the create response**. Save it immediately — it cannot be retrieved again. Use the returned `masked_api_key` to reference the key in your dashboards and logs.
</Warning>
