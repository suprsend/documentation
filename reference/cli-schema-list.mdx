---
title: "List Schemas"
description: "List all notification schemas in your workspace."
keywords:
  - suprsend schema list
  - list schemas suprsend
  - suprsend cli schema
  - list trigger payload validation schema cli
  - suprsend cli reference
---

## Syntax

```bash
suprsend schema list [flags]
```

## Flags

| Flag | Description | Default |
|------|-------------|---------|
| `-h, --help` | Show help for the command | – |  
| `-l, --limit int` | Limit the number of schemas to list| `20` |
| `-m, --mode string` | Mode of schemas to list (draft, live) | `live` |
| `-f, --offset int` | Offset the number of schemas to list | `0` |
| `-o, --output string` | Output Style (pretty, yaml, json) | `pretty` |


## Example

```bash
# List schemas in staging workspace (default)
suprsend schema list

# List schemas in production workspace
suprsend schema list --workspace production

# List schemas in draft mode, with limit and offset
suprsend schema list --limit 10 --offset 5 --mode draft

# List schemas in JSON format and pipe to jq
suprsend schema list --output json | jq '.[].slug'
```
