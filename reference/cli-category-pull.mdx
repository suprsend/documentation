---
title: "Pull Categories"
description: "Fetch categories from SuprSend workspace to local directory"
keywords:
  - suprsend preference category pull
  - suprsend cli manage preference categories
  - suprsend cli reference
  - git version control changes in preference categories
  - sync suprsend categories in git
---

This action does not delete existing categories; instead, it overwrites categories with the same slug or creates new ones. <br/><br/>
By default, pulled categories are stored in the `suprsend/category/` directory, but you can change the destination using `-d, --dir` flag. We recommend specifying a directory when working with multiple production or staging workspaces.

## Syntax

```bash
suprsend category pull [flags]
```

## Flags

| Flag | Description | Default |
|------|-------------|---------|
| `-h, --help` | Show help for the command | – |
| `-d, --dir string` | Output directory for categories | `./suprsend/category` |
| `-m, --mode string` | Mode to pull categories from | `live` |
| `-w, --workspace string` | Workspace to pull categories from | `staging` |

## Example

```bash
# Pull all live categories to default directory
suprsend category pull

# Pull categories into custom directory path
suprsend category pull --dir categories

# Pull categories from production workspace
suprsend category pull --workspace production

# Pull draft categories
suprsend category pull --mode draft
```
