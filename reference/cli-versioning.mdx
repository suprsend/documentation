---
title: "Versioning and Support Policy"
description: "Learn about SuprSend's versioning and support policy for the CLI tool."
keywords: ["CLI versioning", "semantic versioning", "CLI compatibility", "CLI migration", "SuprSend CLI", "version policy", "breaking changes", "support policy"]
---


<CardGroup cols={3}>
  <Card title="Changelog" icon="list" iconType="solid" href="/reference/cli-changelog">
    View all CLI releases and updates
  </Card>
  
  <Card title="Installation Guide" icon="download" iconType="solid" href="/reference/cli-installation">
    Step-by-step installation instructions
  </Card>
  
  <Card title="CLI Documentation" icon="book" iconType="solid" href="/reference/cli-intro">
    Complete CLI integration guides
  </Card>
</CardGroup>

<Warning>
**Beta Release:** The SuprSend CLI is currently in beta. Features are still evolving and may change before production readiness. We recommend testing thoroughly in staging environments before using in production.
</Warning>

## Versioning Policy

SuprSend's CLI versioning policy follows **[Semantic Versioning (SemVer)](https://semver.org/)**. For example, in version 0.2.15, 0 is the **major**, 2 is the **minor**, and 15 is the **patch**.

**Major.** Breaking changes that are backward incompatible (e.g., renaming commands, changing command structure, modifying profile format).

**Minor.** New features that are backward compatible (e.g., adding new commands, optional flags, new subcommands).

**Patch.** Backward-compatible bug fixes (e.g., fixing command execution errors, improving error messages, performance optimizations).

Each CLI version is coupled with the API version that was current at the time of release, ensuring stable and predictable behavior across versions.

## Support Policy

All new features, performance improvements, and bug fixes are released on the latest major version. Older major versions remain functional but don't receive updates.

SuprSend maintains backward compatibility across CLI versions, ensuring existing commands, workflow operations, profile configurations, and CI/CD scripts continue to work. We recommend upgrading to the latest version to access new commands, improved authentication, better performance, and new integrations like translations and type generation.

CLI and API versions are synchronized, ensuring CLI commands always work with the current API and new platform features are immediately available via CLI. When upgrading between major versions, detailed changelogs and migration notes are provided.

## Platform Support

The CLI is available for macOS (arm64, x64), Linux (arm64, x86_64), and Windows (arm64, x86_64). All platforms receive full support with regular updates and security patches.

For installation instructions, see the [Installation Guide](/reference/cli-installation).

## Release Policy

New CLI features are released as soon as development and testing are complete, typically at a rate of ~1-2 per week. New versions include new commands, feature enhancements, bug fixes, and security updates. User-facing commands maintain backward compatibility within the same major version.

Critical bug fixes (command execution failures, authentication problems, sync errors) are released immediately, while minor improvements are batched for regular releases. Releases are announced on our [Slack community](https://suprsend.com/slack) and via [email newsletters](https://suprsend.com/newsletter). See the [CLI Changelog](/reference/cli-changelog) for detailed release notes.

## Best Practices

**Before Upgrading:**
- Test CLI commands in staging environment first
- Review [changelog](/reference/cli-changelog) for breaking changes
- Backup CLI profiles and configurations
- Verify CI/CD scripts are compatible

**After Upgrading:**
- Run `suprsend --version` to verify installation
- Test critical commands (workflow list, schema pull, etc.)
- Test workflow push/pull and schema operations
- Update pinned versions in CI/CD pipelines if needed

**Important Notes:**
- Profiles are preserved across upgrades, but major version changes may require recreation
- Verify shell aliases and wrapper scripts still work after upgrade
- For production, test new versions in a separate environment first

<Note>
💡 **Pro tip:** Start with patch upgrades before moving to minor or major versions. Always check the [changelog](/reference/cli-changelog) before upgrading.
</Note>

