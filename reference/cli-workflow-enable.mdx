---
title: "Enable Workflow"
description: "Enable a workflow in a SuprSend workspace to start processing notifications."
keywords:
  - suprsend workflow enable
  - enable workflow suprsend
  - activate workflow
---

All pushed workflows are enabled by default.
Workflows have to be live to be enabled.


## Syntax

```bash
suprsend workflow enable <workflow-slug> [flags]
```

**Arguments:**
- `<workflow-slug>` - Slug of the workflow to enable (required)

## Example

```bash
# Enable workflow in staging workspace
suprsend workflow enable my-workflow

# Enable workflow in another workspace
suprsend workflow enable user-signup --workspace production
```