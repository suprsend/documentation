---
title: "Global Flags"
description: "Reference for global flags available in the SuprSend CLI. These flags can be used with any command."
keywords:
  - suprsend cli flags
  - suprsend global flags
  - suprsend command line options
  - suprsend cli configuration
  - suprsend output formats
  - suprsend verbosity
---

## Options

| Flag | Default | Env Var |
|------|---------|---------|
| `--config string` | `$HOME/.suprsend.yaml` | – |
| `-h, --help` | – | – |
| `-n, --no-color` | Disabled | `NO_COLOR` |
| `-o, --output string` | `pretty` | – |
| `-s, --service-token string` | – | `SUPRSEND_SERVICE_TOKEN` |
| `-v, --verbosity string` | `info` | – |
| `-w, --workspace string` | `staging` | – |

## Flag Details

### --config
**string**  
Path to configuration file. Defaults to `$HOME/.suprsend.yaml`.

```bash
suprsend --config /path/to/config.yaml workflow list
```

### --help, -h
Show help information for any command.

```bash
suprsend --help
suprsend -h
suprsend workflow --help
suprsend workflow -h
suprsend workflow list --help
suprsend workflow list -h
```

### --no-color, -n
Disable colored output. By default, you get colored output. Can also be set via `NO_COLOR` environment variable.

```bash
suprsend --no-color workflow list
suprsend -n workflow list

# Set globally via environment variable
export NO_COLOR=1
```

### --output, -o
**string**  
Output format: `pretty`, `yaml`, or `json`. Defaults to `pretty`.

```bash
suprsend workflow list --output json
suprsend workflow list -o json
suprsend workflow list --output yaml
suprsend workflow list -o yaml
```

### --service-token, -s
**string**  
Service token for authentication. Can also be set via `SUPRSEND_SERVICE_TOKEN` environment variable.

```bash
suprsend workflow list --service-token "your_token_here"
suprsend workflow list -s "your_token_here"
```

### --verbosity, -v
**string**  
Log level: `debug`, `info`, `warn`, `error`, `fatal`, or `panic`. Defaults to `info`.

```bash
suprsend workflow list --verbosity debug
suprsend workflow list -v debug
suprsend workflow list --verbosity warn
suprsend workflow list -v warn
```

### --workspace, -w
**string**  
Target workspace for commands. Defaults to `staging`.

```bash
suprsend --workspace production workflow list
suprsend -w production workflow list
suprsend --workspace staging schema list
suprsend -w staging schema list
```