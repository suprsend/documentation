---
title: "List MCP Tools"
description: "Command reference for listing all tools supported by the SuprSend MCP server."
keywords:
  - suprsend mcp tools
  - list mcp tools
  - suprsend cli reference
  - ai agents suprsend
---

List all the tools supported by the MCP server.

```bash
suprsend list-tools [flags]
```

## Options

| Flag | Description |
|------|-------------|
| `-h, --help` | Show help for the command |

### Inherited Global Flags

This command also supports [Global Flags](/reference/cli-global-flags), such as:

* `--config` – Config file (default: `$HOME/.suprsend.yaml`)
* `--no-color` – Disable color output
* `--output` – Output format (`pretty`, `json`, `yaml`)
* `--service-token` – Service token for authentication
* `--verbosity` – Logging level

## Examples

List all available MCP tools:

```bash
suprsend list-tools
```

List tools with JSON output:

```bash
suprsend list-tools --output json
```

## See Also

* [Start MCP Server](/reference/cli-mcp-server) – run the MCP server
* [MCP Tools Overview](/reference/cli-mcp-overview) – overview of MCP framework
* [Authentication](/reference/cli-authentication) – how to authenticate the CLI
* [Profiles](/reference/cli-profile-overview) – manage multiple environments