---
title: "Internationalization"
description: "Translate inbox UI strings into your user's language in JavaScript applications."
---

The `locale` configuration option translates inbox UI labels — such as "Notifications", "Mark all as read", and time-relative strings — into the user's language.

## Setup

Pass the `locale` code in the `initSuprSend` configuration:

```javascript
import { initSuprSend } from "@suprsend/web-components";

initSuprSend({
  publicApiKey: "YOUR_PUBLIC_API_KEY",
  distinctId: "USER_DISTINCT_ID",
  userToken: "SIGNED_USER_TOKEN",
  locale: "fr",
});
```

## Supported languages

The following languages are supported out of the box:

| Code | Language |
|------|----------|
| `en` | [English](https://github.com/suprsend/suprsend-react-core/blob/main/src/i18n/languages/en.ts#L5) (default) |
| `fr` | [French](https://github.com/suprsend/suprsend-react-core/blob/main/src/i18n/languages/fr.ts#L5) |
| `de` | [German](https://github.com/suprsend/suprsend-react-core/blob/main/src/i18n/languages/de.ts#L5) |
| `es` | [Spanish](https://github.com/suprsend/suprsend-react-core/blob/main/src/i18n/languages/es.ts#L5) |
| `ar` | [Arabic](https://github.com/suprsend/suprsend-react-core/blob/main/src/i18n/languages/ar.ts#L5) |

## Custom translations

To add an unsupported language or override specific strings, pass a `translations` object:

```javascript
initSuprSend({
  publicApiKey: "YOUR_PUBLIC_API_KEY",
  distinctId: "USER_DISTINCT_ID",
  userToken: "SIGNED_USER_TOKEN",
  locale: "fr",
  translations: {
    notifications: "Mes notifications",
    markAllAsRead: "Tout marquer comme lu",
  },
});
```

### Available translation keys

```typescript
interface ITranslations {
  notifications?: string;
  markAllAsRead?: string;
  noNotificationsTitle?: string;
  noNotificationsDescription?: string;
  pinned?: string;
  markAsUnread?: string;
  markAsRead?: string;
  archive?: string;
  expiresIn?: string;
  minute?: string;
  minutes?: string;
  hour?: string;
  hours?: string;
  day?: string;
  days?: string;
  week?: string;
  weeks?: string;
  month?: string;
  months?: string;
  year?: string;
  years?: string;
}
```

## Next steps

<CardGroup cols={2}>
  <Card title="Customising Feed" icon="paintbrush" href="/docs/web-components-customisations">
    Theme colors, layout, and CSS style overrides.
  </Card>
  <Card title="Popover Inbox" icon="bell" href="/docs/web-components-popover-feed">
    Add the bell-icon popover inbox component.
  </Card>
  <Card title="In-App Inbox Template" icon="palette" href="/docs/in-app-inbox-template">
    Design notification templates on the SuprSend dashboard.
  </Card>
</CardGroup>
