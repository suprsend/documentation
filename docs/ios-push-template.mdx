---
title: "iOS Push"
description: "Create iOS push notifications with deep links, rich images, and a live lock screen preview."
---

iOS Push in SuprSend lets you send rich push notifications to your iOS users — with titles, body text, images, deep links, and dynamic variables. A live lock screen preview updates as you build, so you see what recipients will see before you send.

This guide covers every field in the editor, how variables work, and how to handle iOS-specific behaviour around images, Universal Links, and badge counts.

<Note>
  You need an iOS Push vendor connected and the SuprSend SDK integrated in your app before notifications can be sent. [Set up iOS Push →](/docs/ios-push-vendor-integration)
</Note>

---

## How the iOS Push editor works

The editor is a structured form — title, body, image URL, and action URL — with a live iOS lock screen preview on the right that updates as you type.

<Frame caption="iOS Push editor with live lock screen preview">
  <img src="/images/docs/templates-editor-ios-push.png" />
</Frame>

<Frame caption="iOS Push — title, body, rich image, action URL, and advanced options">
  <img src="/images/docs/templates-editor-ios-push-panel.png" />
</Frame>

Keep in mind that the editor preview is approximate. Final rendering depends on the device, iOS version, and whether your app has a Notification Service Extension installed. For critical templates, test on a real device — see [Best practices](#best-practices).

---

## Fields

### Title

**Required** · Supports variables

Single-line heading shown on the lock screen and in Notification Center. iOS truncates to one line, so front-load the key fact. Stay under ~30 characters for predictable display.

Supports Handlebars: `{{variable_name}}`

> Example: `Your order is out for delivery` · `{{actor_name}} liked your post`

### Body

**Required** · Supports variables

Multi-line message text. Only the first line shows in collapsed view — put the critical detail there. Expanded view shows about four lines.

> Example: `Order #{{order_id}} will arrive by {{eta}}. Tap to track.`
> Example: `"{{post_excerpt}}"` for social notifications
> Example: `It might be time to restock {{product_name}}.` for reminders

### Image URL

**Optional** · Supports variables

Public HTTPS URL for a rich notification image. Supported formats: PNG, JPG, JPEG (min 1024×1024 for best rendering). GIFs play in the expanded view.

Requires a **Notification Service Extension** in your app — the SuprSend iOS SDK includes this by default. Large images delay appearance in the expanded view, so keep files small and host on a fast CDN.

See [iOS SDK integration](/docs/ios-integration).

### Action URL

**Optional** · Supports variables

URL opened when the user taps the notification. Without an Action URL, tapping opens the app's default screen — set one whenever the user should land on a specific screen.

Prefer [Universal Links](https://developer.apple.com/ios/universal-links/) over custom URL schemes — they work as both a web fallback and an in-app deep link. See [How do deep links work in iOS push?](#how-do-deep-links-work-in-ios-push) in the FAQ.

> Example: `https://yourapp.com/track/{{order_id}}`

---

## Personalise with variables

You can add variables in the template to personalise it per recipient. SuprSend replaces variables with actual data at send time.

<Steps>
  <Step title="Add variables in the Variables panel">
    Add sample data in the **Variables panel** (Input Payload section) on the left side of the editor. This powers auto-suggestions and the live preview. For the full guide, see [Adding dynamic content](/docs/templates#the-variables-panel).
  </Step>

  <Step title="Use variables in the template">
    Type `{{` in any field — matching variables appear as auto-suggestions. You can also type variable names manually following [Handlebars syntax](https://handlebarsjs.com/guide/#what-is-handlebars).

    **Examples using this sample data:**

    <CodeGroup>
      ```json json
      {
        "event": {
          "location": {
            "city": "San Francisco",
            "state": "California"
          },
          "order_id": "11200123",
          "first_name": "Joe"
        },
        "product_image_url": "https://yourapp.com/images/product.jpg"
      }
      ```
    </CodeGroup>

    - **Nested variable:** `{{event.location.city}}` → renders as `San Francisco`
    - **Variable with space in name:** `{{event.[first name]}}`

    The preview section shows sample values rendered in real-time. If a variable isn't rendering, check:
    1. The variable is defined in the Variables panel.
    2. The variable name matches the Handlebars syntax exactly.
  </Step>
</Steps>

<Warning>
  If a variable cannot be rendered at send time (missing or mismatched data), SuprSend skips that notification for the affected user. Other channels in the same template group are still sent.
</Warning>

---

## Preview and test

Preview and test before you rely on this template in production.

The right panel shows a live iOS lock screen preview, updated in real time as you edit. Variables render using data from the **Variables panel**.

Click **Test** in the top-right corner to send a real push notification to a real device. This uses the **live version** — commit your changes before testing. See [Testing a Template](/docs/templates#test) for the full guide.

---

## Publish

Click **Commit** in the top bar to publish the current draft as a new live version. Add an optional description for versioning. Once committed, all notifications triggered after this point use the new content.

---

## Best practices

- **Same bar as Android:** Use ~40 characters for title and ~80 for body for cross-platform templates — iOS shows more body on the lock screen, but the short spec keeps Android and iOS aligned.
- **Action URL required:** Every notification should open a useful screen — prefer Universal Links (`https://…`) over custom schemes so links work when the app is not installed.
- **OTP and verification:** Do not show the code in title or body — only neutral copy like "Your code is ready"; lock screens must not expose secrets.
- **Rich media:** Rich images need a **Notification Service Extension** (included with the SuprSend iOS SDK). Test on a real device — previews are approximate.
- **Badge:** SuprSend increments badge by default; override from your backend if the badge should match unread count, not raw deliveries.
- **Images:** Banner for promo-style sends; skip for transactional and auth. Use CDN URLs for dynamic images and keep files small for LTE.

---

## FAQ

<AccordionGroup>
  <Accordion title="Why aren't images showing in my push notification?">
    Your app needs a **Notification Service Extension** to display rich media (images, GIFs). The SuprSend iOS SDK includes this by default. If you're not using the SDK, add the extension manually. Supported formats: JPEG, PNG, GIF. Max size: 10 MB. Images display in the expanded notification view (long-press or swipe down). See [iOS SDK integration](/docs/ios-integration).
  </Accordion>

  <Accordion title="How do deep links work in iOS push?">
    The **Action URL** supports three URL types:

    | URL type | Format | Behaviour |
    |---|---|---|
    | Web URL | `https://yourapp.com/orders/123` | Opens in Safari (or your app if [Universal Links](https://developer.apple.com/ios/universal-links/) are configured) |
    | Custom scheme | `yourapp://orders/123` | Opens the matching view controller in your app |
    | Universal Link | `https://yourapp.com/orders/123` (Associated Domains) | Opens your app directly |

    Prefer Universal Links — they work as both web fallback and in-app deep links.
  </Accordion>

  <Accordion title="How does badge count work?">
    SuprSend increments the app badge by 1 for each notification. Clear the badge in your app when the user opens it or reads notifications. For an accurate unread count, set the badge from your backend using `setBadgeCount` (iOS 16+) or `applicationIconBadgeNumber`.
  </Accordion>

  <Accordion title="What happens if a variable is missing or has the wrong value at send time?">
    SuprSend skips the iOS Push notification for that user. Other channels in the same template group are still sent if they render successfully.
  </Accordion>

  <Accordion title="Can I use this channel alongside other channels in the same workflow?">
    Yes — iOS Push is one channel in a template group; other channels send independently.
  </Accordion>

  <Accordion title="How do I pass variable values when triggering the workflow?">
    Include properties in your trigger payload so Handlebars paths match. Use the **Variables panel** for sample data. See [Adding dynamic content](/docs/templates#the-variables-panel).
  </Accordion>
</AccordionGroup>