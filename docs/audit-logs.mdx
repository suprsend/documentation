---
title: "Audit Logs"
description: "Learn more about audit logs of actions performed on your SuprSend account."
---

Audit logs provide a complete record of all actions performed by team members on your SuprSend account. Each entry includes who performed the action, when it happened, and details about what changed.

This is particularly useful for maintaining team accountability, tracking security-sensitive operations, and quickly identifying who made specific changes—especially helpful when someone accidentally deletes a service token or makes an unintended configuration change.

<Note>
  Audit logs are available on the Enterprise plan. Access is restricted to admins and account owners.
</Note>

## Accessing audit logs

To view all account activity:

1. Go to **Account Settings** in the SuprSend dashboard
2. Click on the [Audit Logs tab](https://app.suprsend.com/en/account-settings/audit-logs)

The audit logs page displays a list showing the actor, action, workspace, location, and date for each entry.

![](/images/audit-logs.png)

Filter and search audit logs to find specific entries:

- **Search**: Find particular actions or events
- **Filter by date range**: Choose relative date ranges (e.g., last 7 days, last 30 days) or set an absolute date range
- **Filter by actor**: View actions performed by specific team members
- **Filter by action**: Narrow down to specific action types (e.g., `service_token.deleted`, `vendor.added`)

## Tracked actions

Audit logs track platform-related actions including account management, security operations, configuration changes, integration management, and billing modifications. Coverage is continuously expanding to include workflow management, template changes, category management, and tenant operations.

<Accordion title="View all tracked actions" defaultOpen={false}>
The following table lists all actions currently tracked in audit logs:

| Product Page | Action | Targets | Metadata | Description | Notes |
| --- | --- | --- | --- | --- | --- |
| **Account Settings → General** | `account.general_settings_modified` | organization | old, new | Timezone changed to **&#123;&#123;metadata.new.timezone&#125;&#125;** | Only tracked when timezone is updated |
| **Account Settings → Team** | `account.member_invited` | user |  | Invite sent to **&#123;&#123;targets.user.email&#125;&#125;** |  |
| **Account Settings → Team** | `account.member_deactivated` | user |  | **&#123;&#123;targets.user.email&#125;&#125;** account deactivated |  |
| **Account Settings → Team** | `account.member_invite_cancelled` | user |  | Invite cancelled for **&#123;&#123;targets.user.email&#125;&#125;** |  |
| **Account Settings → Team** | `account.member_invite_resent` | user |  | Invite resent to **&#123;&#123;targets.user.email&#125;&#125;** |  |
| **Account Settings → Team** | `account.member_role_changed` | user | old_value, new_value | Changed role of **&#123;&#123;targets.user.email&#125;&#125;** from &#123;&#123;metadata.previous_role&#125;&#125; to &#123;&#123;metadata.new_role&#125;&#125; |  |
| **Account Settings → Authentication** | `account.mfa_enabled` | organization |  | Multi-factor authentication enabled |  |
| **Account Settings → Authentication** | `account.mfa_disabled` | organization |  | Multi-factor authentication disabled |  |
| **Account Settings → Billing** | `account.billing_modified` | pricing_plan |  | Pricing plan changed to **&#123;&#123;targets.pricing_plan.plan&#125;&#125;** |  |
| **Account Settings → Service Tokens** | `service_token.generated` | service_token |  | Generated token **- &#123;&#123;targets.auth_key.name&#125;&#125;** |  |
| **Account Settings → Service Tokens** | `service_token.deleted` | service_token |  | Deleted token **- &#123;&#123;targets.auth_key.name&#125;&#125;** |  |
| **Developers → API Keys** | `api_key.generated` | api_key |  | Generated key **- &#123;&#123;targets.auth_key.name&#125;&#125;** |  |
| **Developers → API Keys** | `api_key.deleted` | api_key |  | Deleted key **- &#123;&#123;targets.auth_key.name&#125;&#125;** |  |
| **Developers → API Keys** | `public_key.generated` | public_key |  | Generated key **- &#123;&#123;targets.auth_key.name&#125;&#125;** |  |
| **Developers → API Keys** | `public_key.rotated` | public_key |  | Rotated key **- &#123;&#123;targets.auth_key.name&#125;&#125;** |  |
| **Developers → API Keys** | `public_key.deleted` | public_key |  | Deleted key **- &#123;&#123;targets.auth_key.name&#125;&#125;** |  |
| **Developers → API Keys** | `public_key.security_mode_enabled` | public_key |  | Enabled security mode for **&#123;&#123;targets.auth_key.name&#125;&#125;** key |  |
| **Developers → API Keys** | `public_key.security_mode_disabled` | public_key |  | Disabled security mode for **&#123;&#123;targets.auth_key.name&#125;&#125;** key |  |
| **Developers → API Keys** | `signing_key.generated` | signing_key |  | Generated key **- &#123;&#123;targets.signing_key.id&#125;&#125;** |  |
| **Developers → API Keys** | `signing_key.deleted` | signing_key |  | Deleted key **- &#123;&#123;targets.signing_key.id&#125;&#125;** |  |
| **Developers → API Keys** | `signing_key.rolled` | signing_key | old, new | Rolled key **- &#123;&#123;targets.signing_key.id&#125;&#125;** |  |
| **Developers → Webhooks** | `webhook.added` | webhook |  | Added endpoint - **&#123;&#123;targets.webhook.endpoint&#125;&#125;** |  |
| **Developers → Webhooks** | `webhook.modified` | webhook | old, new | Modified configuration for endpoint - **&#123;&#123;targets.webhook.endpoint&#125;&#125;** | Not tracked if only description is modified |
| **Developers → Webhooks** | `webhook.enabled` | webhook |  | Enabled endpoint - **&#123;&#123;targets.webhook.endpoint&#125;&#125;** |  |
| **Developers → Webhooks** | `webhook.disabled` | webhook |  | Disabled endpoint - **&#123;&#123;targets.webhook.endpoint&#125;&#125;** |  |
| **Connectors** | `connector.added` | connector |  | Added **&#123;&#123;targets.connector.integration&#125;&#125;** connector - **&#123;&#123;targets.connector.name&#125;&#125;** |  |
| **Connectors** | `connector.modified` | connector |  | Modified **&#123;&#123;targets.connector.integration&#125;&#125;** connector - **&#123;&#123;targets.connector.name&#125;&#125;** | Not logged if only the name is modified |
| **Connectors** | `connector.enabled` | connector |  | Enabled **&#123;&#123;targets.connector.integration&#125;&#125;** connector - **&#123;&#123;targets.connector.name&#125;&#125;** |  |
| **Connectors** | `connector.disabled` | connector |  | Disabled **&#123;&#123;targets.connector.integration&#125;&#125;** connector - **&#123;&#123;targets.connector.name&#125;&#125;** |  |
| **Vendors** | `vendor.added` | tenant, channel, vendor |  | Added &#123;&#123;targets.channel.name&#125;&#125; vendor in &#123;&#123;targets.tenant.id&#125;&#125; tenant - &#123;&#123;targets.vendor.integration&#125;&#125; (**&#123;&#123;targets.vendor.name&#125;&#125;**) |  |
| **Vendors** | `vendor.modified` | tenant, channel, vendor |  | Modified &#123;&#123;targets.channel.name&#125;&#125; vendor in &#123;&#123;targets.tenant.id&#125;&#125; tenant - &#123;&#123;targets.vendor.integration&#125;&#125; (**&#123;&#123;targets.vendor.name&#125;&#125;**) |  |
| **Vendors** | `vendor.active_list_modified` | tenant, channel |  | Active vendor list modified for &#123;&#123;targets.channel.name&#125;&#125; vendor in &#123;&#123;targets.channel.category&#125;&#125; category |  |

<Note>
  **📘 Understanding action descriptions:**
  
  Action descriptions use template variables (e.g., `&#123;&#123;targets.user.email&#125;&#125;`, `&#123;&#123;metadata.new.timezone&#125;&#125;`) that are replaced with actual values when the action is logged. This provides context-specific information for each event.
</Note>
</Accordion>

## FAQs

<AccordionGroup>
  <Accordion title="Why do I see system actions in my audit logs?" defaultOpen={false}>
    When events occur that do not originate from a user action (like automated system updates or side effects from merges), these events may be attributed to system processes. This helps maintain a complete audit trail of all changes to your account.
  </Accordion>

  <Accordion title="Can I export my audit logs?" defaultOpen={false}>
    Please contact the SuprSend support team if you need to export the data inside your audit logs. We'd be happy to assist.
  </Accordion>

  <Accordion title="How far back do audit logs go?" defaultOpen={false}>
    Audit log retention depends on your pricing plan. Check the [pricing page](https://www.suprsend.com/pricing) to see the log retention timeframe applicable for your plan.
  </Accordion>
</AccordionGroup>
