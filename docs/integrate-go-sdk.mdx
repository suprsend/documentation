---
title: "Integrate Go SDK"
description: "Install & Initialize SuprSend Go SDK using your workspace credentials for sending notifications."
---

## Installation

Install `suprsend-go` sdk

<CodeGroup>

```shell bash
go get github.com/suprsend/suprsend-go
```

</CodeGroup>

## Initialization

For initializing SDK, you need Server-Side api key and secret. You will get both the tokens from your [Suprsend dashboard](https://app.suprsend.com/en/production/developers/api-keys) (Developers -> API Keys).

<CodeGroup>

```go Request
package main

import (
  "log"

  suprsend "github.com/suprsend/suprsend-go"
)

// Initialize SDK
func main() {
  opts := []suprsend.ClientOption{
    // suprsend.WithDebug(true),
  }
  suprClient, err := suprsend.NewClient("__api_key__", "__api_secret__", opts...)
  if err != nil {
    log.Println(err)
  }
}
```

</CodeGroup>

---
