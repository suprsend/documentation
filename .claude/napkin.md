# Napkin (suprsend-docs)

## Session notes

- `user-suprsend` MCP `list_workflows` failed: Invalid service token — expected without dashboard credentials; workflow was authored from OpenAPI + docs fetch instead.
- Cart abandonment workflow saved at `suprsend/workflow/cart-abandonment.json` — email + `inbox` (in-app), 1h `branch_waituntil`, early exit on `cart_completed` + `cart_id` match.
